

This is a simple calculator that runs on a server. User input is submitted through the user's webpage and the calculation is done on the server. The calculated value is then sent to the user and is put on the user's webpage.